/**
 * @summary Internal API routes configuration
 * @module routes/v1/internalRoutes
 */

import { Router } from 'express';

const router = Router();

/**
 * @summary Authenticated routes will be added here
 */

export default router;
