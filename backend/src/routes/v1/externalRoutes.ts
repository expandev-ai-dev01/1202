/**
 * @summary External API routes configuration
 * @module routes/v1/externalRoutes
 */

import { Router } from 'express';

const router = Router();

/**
 * @summary Public routes will be added here
 */

export default router;
